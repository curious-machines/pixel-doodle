# Gradient — simplest pixel kernel

pipeline pd {
  pixel kernel "gradient.pd"
  display gradient
}

pipeline pdir {
  pixel kernel "gradient.pdir"
  display gradient
}

pipeline gpu {
  pixel kernel "gradient.wgsl"
  display gradient
}
