# Texture sampling test — loads a PNG and displays it mapped to the viewport

pipeline pd {
  pixel kernel "texture_test.pd"
  texture img = file("../../images/lake-2.jpg")
  run texture_test
  display
}
