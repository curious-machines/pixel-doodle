# Mandelbrot with pan/zoom

include "../../shared/pan_zoom.pdp"

pipeline pd {
  pixel kernel "mandelbrot.pd"
  run mandelbrot
  display
}

pipeline pdir {
  pixel kernel "mandelbrot.pdir"
  run mandelbrot
  display
}

pipeline gpu {
  pixel kernel "mandelbrot.wgsl"
  run mandelbrot
  display
}
