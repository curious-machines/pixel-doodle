include "../../shared/pan_zoom.pdp"

pipeline pd {
  pixel kernel "circle.pd"
  display circle
}

pipeline pdir {
  pixel kernel "circle.pdir"
  display circle
}

pipeline gpu {
  pixel kernel "circle.wgsl"
  display circle
}
