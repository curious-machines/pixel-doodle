include "../../shared/pan_zoom.pdp"

pipeline pd {
  pixel kernel "cornell_2d.pd"
  accumulate(samples: 512) {
    run cornell_2d
    display
  }
}

pipeline pdir {
  pixel kernel "cornell_2d.pdir"
  accumulate(samples: 512) {
    run cornell_2d
    display
  }
}

pipeline gpu {
  pixel kernel "cornell_2d.wgsl"
  run cornell_2d
  display
}
