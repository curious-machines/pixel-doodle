import { register_pan_zoom } from "../../shared/pan_zoom"

var pixels = Buffer.U32()
var kern = Kernel.Pixel("sdf3d_demo", "sdf3d_demo.wgsl")

fn init() {
    register_pan_zoom()
}

fn frame() -> bool {
    kern.output = Bind.Out(pixels)
    kern.run()
    pixels.display_buffer()
    return false
}
