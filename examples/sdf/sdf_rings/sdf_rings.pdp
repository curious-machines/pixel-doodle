include "../../shared/pan_zoom.pdp"

pipeline pd {
  pixel kernel "sdf_rings.pd"
  accumulate(samples: 256) {
    display sdf_rings
  }
}

pipeline pdir {
  pixel kernel "sdf_rings.pdir"
  accumulate(samples: 256) {
    display sdf_rings
  }
}

pipeline gpu {
  pixel kernel "sdf_rings.wgsl"
  display sdf_rings
}
