include "../../shared/pan_zoom.pdp"

pipeline pd {
  pixel kernel "sdf.pd"
  run sdf
  display
}

pipeline pdir {
  pixel kernel "sdf.pdir"
  run sdf
  display
}

pipeline gpu {
  pixel kernel "sdf.wgsl"
  run sdf
  display
}
